# CSV to Formatted XLSX 開發白皮書

> 本文件是提供給 VS Code 內 AI 開發代理使用的專案規格。除非使用者明確修改需求，AI 應依照本文件實作、測試及封裝，不得自行刪減核心資料保護規則。

## 1. 專案基本資料

| 項目 | 名稱 |
|---|---|
| GitHub Repository | `csv-to-formatted-xlsx` |
| 專案資料夾 | `csv-to-formatted-xlsx` |
| Windows 程式名稱 | `CSV報表轉換工具.exe` |
| 主要用途 | 將 Looker Studio／Data Studio 匯出的 CSV 批次轉換成已有正確格式的 XLSX |
| 主要使用者 | 不熟悉 CSV、Excel 欄位格式及 Python 的一般公司人員 |
| 目標平台 | Windows 10、Windows 11，64 位元 |
| 操作方式 | 將 CSV 放入 `in`，雙擊 EXE，從 `out` 取得 XLSX |

## 2. 背景與問題定義

Looker Studio 下載的表格是沒有 Excel 儲存格格式的 CSV。使用者直接以 Excel 開啟時，Excel 會自行推測資料型別，造成：

- 13 碼或更長的條碼顯示為科學記號。
- 以 `0` 開頭的條碼遺失前導 0。
- 長數字可能超過 Excel 15 位有效數字限制而被永久改值。
- 混合文字的識別碼可能產生不一致格式。
- 欄寬、篩選、凍結窗格和數值顯示均需人工整理。

本工具必須直接解析 CSV 原始文字，在任何數值轉型發生前辨識需保護的欄位，並輸出具明確儲存格格式的 XLSX。

## 3. 實際輸入資料特性

目前範例報表具有以下欄位：

```text
訂單年, 月, 平台, 品牌, 系列, 貨號, 品名, 包裝值, 包裝單位, 條碼, 數量, % Δ, 銷售額, % Δ
```

實際條碼內容不只標準 EAN：

- 13 碼：`9003579000366`
- 前導 0：`02131137`
- 文字：`無資料`
- 數字加註記：`02131356短效`
- 數字加註記：`5060412219549三包`
- 長度可能為 8、12、13、14、15 碼或其他長度

因此不得以「是否全為數字」或固定長度判斷條碼。只要欄名符合保護規則，整欄必須從讀取到寫入都維持字串。

## 4. 專案目標

### 4.1 MVP 必須完成

1. 掃描程式旁的 `in` 資料夾內所有 `.csv` 檔案。
2. 每個 CSV 各自輸出一個同名 `.xlsx` 至 `out`。
3. 正確處理 UTF-8 BOM；並對常見台灣 CSV 編碼提供安全的備援偵測。
4. 保護「條碼」欄，防止科學記號、前導 0 遺失及長數字精度損失。
5. 設定標題、凍結首列、自動篩選、欄寬及基本數值格式。
6. 單一檔案失敗時繼續處理其他檔案。
7. 執行結束顯示成功、略過與失敗結果。
8. 可使用 PyInstaller 封裝成免安裝 EXE，目標電腦不需 Python。

### 4.2 非 MVP

- 不連線至 Looker Studio API。
- 不自動下載報表。
- 不修改原始 CSV。
- 不使用 Microsoft Excel COM，因此不要求安裝 Excel。
- 第一版不做複雜圖形介面、排程、雲端同步或多工作表合併。

## 5. 使用者操作流程

```text
第一次執行
  → 自動建立 in 與 out
  → 提示「請將 CSV 放入 in 資料夾」

一般執行
  → 掃描 in/*.csv
  → 驗證並讀取各檔案
  → 套用欄位格式
  → 暫存輸出
  → 成功後原子性移至 out/*.xlsx
  → 顯示處理摘要
```

建議讓視窗在使用者按 Enter 後才關閉，避免雙擊 EXE 時錯誤訊息一閃而過。另提供 `--no-pause` 供測試或自動化使用。

## 6. 功能需求

### FR-01 路徑解析

- 原始碼執行時，以專案根目錄為基準。
- PyInstaller 執行時，以 EXE 所在資料夾為基準，不得以 `_MEIPASS` 臨時解壓目錄作為 `in/out` 位置。
- 缺少 `in` 或 `out` 時自動建立。
- 資料夾名稱固定為小寫 `in`、`out`。

### FR-02 檔案探索

- 處理 `in` 第一層內副檔名不分大小寫的 CSV。
- 預設依檔名排序處理。
- 忽略暫存檔與非 CSV 檔案。
- `in` 沒有 CSV 時顯示明確提示，不視為程式崩潰。

### FR-03 CSV 解析

- 優先嘗試 `utf-8-sig`。
- 建議依序備援：`utf-8-sig` → `utf-8` → `cp950`；不得以忽略錯誤方式靜默丟字。
- 使用 Python `csv` 模組逐列解析，保留原始字串；不要先用會推測型別的資料框流程處理條碼。
- 支援引號、欄位內逗號及換行等標準 CSV 規則。
- 可用 `csv.Sniffer` 輔助判斷分隔符，但判斷失敗時預設逗號。
- 驗證標題列存在；重複或空白欄名應記錄警告，但不可造成資料靜默錯位。
- 每列欄位數不一致時，該檔案應失敗並提供列號及原因。

### FR-04 欄名正規化與格式規則

比對欄名時可先：

1. 移除 UTF-8 BOM。
2. 去除欄名前後空白。
3. 將內部連續空白正規化。
4. 英文字母採不分大小寫比對。

第一版欄位分類：

| 分類 | 欄名或判斷方式 | XLSX 格式 | 寫入型別 |
|---|---|---|---|
| 受保護文字 | `條碼`，以及可設定的別名 | `@` | `str` |
| 一般文字 | 平台、品牌、系列、貨號、品名、包裝單位等 | `@` 或 General | `str` |
| 整數 | 月、數量等已知欄名，且內容合法 | `#,##0` | `int` |
| 金額 | 銷售額等已知欄名，且內容合法 | `#,##0.00` 或依實際小數保留 | `int/float/Decimal` |
| 百分比 | `% Δ` 等百分比欄位 | 見下節 | 數值或空白 |
| 未知欄位 | 未命中規則 | 保守推測；有疑義時保留文字 | 依安全規則 |

「貨號」「SKU」「訂單編號」也屬識別碼，建議預設為文字。格式規則應集中於設定模組，不得散落在寫檔程式中。

### FR-05 條碼保護——最高優先級

條碼欄必須遵守：

- CSV 讀取結果不得執行 `int()`、`float()` 或任何數值推測。
- 寫入 XLSX 的 Python 值必須是 `str`。
- 每個資料儲存格的 `number_format` 必須為 `@`。
- 不得在值前加入可見的單引號；格式保護應由 XLSX 儲存格型別完成。
- 保留前導 0、尾端空白以外的原始內容、中文註記及任意長度。
- 空字串維持空白，不寫入字面值 `None`、`nan` 或 `NaN`。
- 不得使用公式 `="條碼"`，避免公式注入、相容性與匯出問題。

### FR-06 公式注入防護

CSV 儲存格若以 `=`, `+`, `-`, `@` 開頭，且該欄不是明確數值欄，必須以純文字儲存，不得成為 Excel 公式。應透過字串型別及文字格式防護，並納入測試。

### FR-07 百分比欄位

範例中可能有兩個同名 `% Δ` 欄位，因此格式判斷不得依賴欄名必須唯一。

- 若 CSV 值包含 `%`，例如 `12.5%`，轉為 `0.125` 並設 `0.00%`。
- 若上游輸出已是小數，例如 `0.125`，需依設定決定它代表 12.5%；第一版可採明確設定並在 README 說明。
- 空值維持空白。
- 無法安全解析的內容保留文字並記錄警告，不得擅自改值。

### FR-08 Excel 樣式

- 工作表名稱預設 `報表`，並清理 Excel 禁用字元，最長 31 字。
- 首列加粗、白字、深色底，水平置中。
- 凍結窗格：`A2`。
- 自動篩選範圍涵蓋所有有資料的欄列。
- 標題列高度適當增加。
- 垂直對齊置中；品名等長文字欄靠左。
- 不合併儲存格。
- 欄寬依標題及前若干筆內容估算，設定合理上下限，例如 8–45；中文字寬度需近似加權。
- 數量使用千分位；金額使用千分位並保留必要小數。
- 可選擇加上 Excel Table，但第一版若採用，必須處理重複欄名；否則只使用 AutoFilter。

### FR-09 輸出與覆蓋策略

- 輸出檔名：`來源檔名.xlsx`。
- 若目標不存在，直接建立。
- 若目標已存在，第一版採「安全覆蓋」：先寫入同目錄暫存檔，驗證成功後以 `os.replace` 取代。
- 若 XLSX 正被 Excel 開啟而無法取代，回報該檔失敗，不刪除舊檔。
- 成功後不移動或刪除 `in` 中的 CSV。
- 任一失敗不得留下看似完整的半成品 XLSX。

### FR-10 執行摘要與日誌

終端顯示至少包含：

```text
找到 3 個 CSV
[成功] a.csv → a.xlsx（5,983 列）
[失敗] b.csv：第 24 列欄位數不一致
[成功] c.csv → c.xlsx（120 列）
完成：成功 2、失敗 1
按 Enter 結束...
```

- 建立 `logs/converter.log`，使用 UTF-8，記錄時間、版本、檔名、編碼、列數、警告及例外堆疊。
- 日誌不得寫入整份報表內容；錯誤案例只記錄必要列號及摘要。
- 正常結束回傳碼為 `0`；有任一檔案失敗回傳 `1`；不可恢復的程式錯誤回傳 `2`。

## 7. 非功能需求

### NFR-01 正確性

識別碼保真優先於自動判斷數值的便利性。任何不確定資料都應保留原字串。

### NFR-02 效能

- 目標：一般辦公電腦處理 10 萬列、20 欄 CSV 時不因記憶體耗盡而失敗。
- 建議 `openpyxl.Workbook(write_only=True)` 用於大型檔案，但因欄寬、篩選和樣式需求，需先設計可測試的串流寫入方案。
- MVP 可先採一般 Workbook 支援 1–2 萬列；若採此方案，README 必須說明限制，後續再導入串流模式。

### NFR-03 相容性

- Python 3.11 或 3.12，專案固定一個版本並在 CI 使用同版本。
- 生成的 XLSX 應可由 Microsoft Excel 2019、Microsoft 365 與 LibreOffice 開啟。
- 檔名及路徑支援繁體中文與空白。

### NFR-04 可維護性

- 核心轉換邏輯不得與互動式 `input()` 緊密耦合。
- 功能拆分為讀取、欄位規則、XLSX 寫入、檔案協調、CLI 五層。
- 對外函式加上型別標註及簡潔 docstring。
- 禁止在程式中硬編使用者電腦的絕對路徑。

### NFR-05 安全與隱私

- 全程離線，不上傳報表、不呼叫網路 API。
- 不執行 CSV 內容。
- 防止 Excel 公式注入。
- 不要求系統管理員權限。

## 8. 建議技術選型

| 用途 | 選型 | 原因 |
|---|---|---|
| CSV 解析 | Python 標準庫 `csv` | 可完整控制字串，不產生自動型別推測 |
| XLSX 產生 | `openpyxl` | 能控制文字、數值格式、樣式及篩選 |
| CLI | `argparse` | 標準庫、易測試 |
| 測試 | `pytest` | 易做參數化與回歸測試 |
| EXE 封裝 | `PyInstaller` | 成熟且可產生免安裝執行檔 |
| 程式碼品質 | `ruff` | 快速統一 lint 與格式 |
| 型別檢查 | `mypy`（可選） | 降低重構錯誤 |

不建議以 pandas 作為 MVP 核心，除非所有識別碼欄位在 `read_csv` 時已明確指定 `dtype=str`，且測試證明不會改動原始字串。此專案優先採標準 `csv` 模組。

## 9. 建議目錄結構

```text
csv-to-formatted-xlsx/
├─ in/
│  └─ CSV檔案放這裡.txt
├─ out/
│  └─ .gitkeep
├─ logs/
│  └─ .gitkeep
├─ src/
│  └─ csv_to_formatted_xlsx/
│     ├─ __init__.py
│     ├─ __main__.py
│     ├─ app.py
│     ├─ csv_reader.py
│     ├─ column_rules.py
│     ├─ xlsx_writer.py
│     ├─ paths.py
│     ├─ logging_config.py
│     └─ models.py
├─ tests/
│  ├─ fixtures/
│  ├─ test_csv_reader.py
│  ├─ test_column_rules.py
│  ├─ test_xlsx_writer.py
│  └─ test_end_to_end.py
├─ assets/
│  └─ app.ico
├─ scripts/
│  ├─ build.bat
│  └─ clean_build.bat
├─ .github/
│  └─ workflows/
│     └─ test.yml
├─ .gitignore
├─ LICENSE
├─ README.md
├─ pyproject.toml
├─ requirements.txt
├─ requirements-dev.txt
├─ CSV報表轉換工具.spec
└─ csv-to-formatted-xlsx_開發白皮書.md
```

`in/*.csv`、`out/*.xlsx`、`logs/*.log`、`build/`、`dist/` 應加入 `.gitignore`，但保留說明檔與 `.gitkeep`。

## 10. 模組責任

### `paths.py`

- 判斷 source 與 frozen 執行模式。
- 回傳 EXE／專案基準路徑與 `in/out/logs`。
- 建立必要資料夾。

### `csv_reader.py`

- 編碼選擇、dialect 解析、標題處理、逐列驗證。
- 回傳純字串列與來源中繼資料。
- 不處理 Excel 樣式。

### `column_rules.py`

- 欄名正規化。
- 依欄名和內容決定 `TEXT_PROTECTED`、`TEXT`、`INTEGER`、`DECIMAL`、`PERCENT`。
- 受保護欄位規則優先，任何其他推測都不能覆蓋。

### `xlsx_writer.py`

- 建立 Workbook、寫值、套格式、設定凍結、篩選、欄寬。
- 使用暫存檔及原子性完成輸出。
- 不掃描 `in`。

### `app.py`

- 協調批次處理、錯誤隔離、執行摘要與回傳碼。
- 提供可由測試直接呼叫的 `run(input_dir, output_dir, ...)`。

### `__main__.py`

- 解析 CLI 參數。
- 管理雙擊視窗暫停行為。

## 11. CLI 規格

預設雙擊模式：

```powershell
CSV報表轉換工具.exe
```

進階模式：

```powershell
CSV報表轉換工具.exe --input "D:\報表\in" --output "D:\報表\out" --no-pause
```

建議參數：

| 參數 | 預設 | 說明 |
|---|---|---|
| `--input` | EXE 旁的 `in` | 輸入資料夾 |
| `--output` | EXE 旁的 `out` | 輸出資料夾 |
| `--no-pause` | false | 結束後不等待 Enter |
| `--overwrite` | true | 安全覆蓋同名 XLSX |
| `--version` | — | 顯示版本 |

## 12. 資料轉換演算法

對每個 CSV：

1. 取得來源檔案的穩定路徑及輸出路徑。
2. 依編碼順序嘗試完整解碼；解碼錯誤才改用下一編碼。
3. 解析標題並建立以「欄位索引」為鍵的規則，避免重複欄名問題。
4. 建立 Workbook 與工作表。
5. 將標題以文字寫入並套標題格式。
6. 逐列驗證欄位數。
7. 逐格依規則轉換：
   - 受保護文字：保留字串，`number_format='@'`。
   - 明確整數／金額／百分比：只有完全可解析時才轉數值。
   - 空字串：寫入 `None`。
   - 其他：保留字串，防止公式執行。
8. 設定凍結、篩選、列高及欄寬。
9. 儲存至 `out` 內的唯一暫存檔。
10. 重新以 openpyxl 載入暫存檔完成最低限度結構驗證。
11. 使用 `os.replace` 發布為正式檔。
12. 記錄成功、列數及警告。

## 13. 測試與驗收標準

### 13.1 單元測試

- BOM 欄名可正確去除。
- UTF-8、UTF-8 BOM、CP950 可正確讀取。
- 前導 0 條碼原樣保留。
- 13、15、18 位純數字識別碼不改值。
- `無資料`、`短效`、`三包`原樣保留。
- 包含逗號、雙引號、換行的 CSV 欄位可正確解析。
- 重複 `% Δ` 欄位均套用百分比規則。
- 空值不變成 `None` 字樣或 `nan`。
- 惡意值 `=HYPERLINK(...)` 不被寫成公式。
- 欄位數不一致時回報正確列號。

### 13.2 XLSX 回讀驗證

以 `openpyxl.load_workbook(..., data_only=False)` 回讀，確認：

- `02131137` 的值仍為字串 `02131137`。
- 對應儲存格 `data_type == 's'`。
- 對應儲存格 `number_format == '@'`。
- `9003579000366` 未變為科學記號或浮點數。
- 18 位測試識別碼每一位均未改變。
- 凍結窗格是 `A2`。
- AutoFilter 範圍涵蓋完整資料。
- 工作簿能正常重新開啟，且不存在修復警告。

### 13.3 端對端驗收

使用提供的實際報表樣本測試：

- 5,983 筆資料全部輸出。
- 欄位數與來源相同。
- 條碼欄 5,983 筆均以文字處理。
- 至少抽驗：`9003579000366`、`02131137`、`02131356短效`、`5060412219549三包`、`無資料`。
- Excel 開啟後條碼欄不顯示科學記號，前導 0 保留。

### 13.4 EXE 驗收

在未安裝 Python 的乾淨 Windows 10／11 環境中：

- 雙擊可啟動。
- 可在中文及含空白路徑執行。
- 自動建立 `in/out/logs`。
- 可處理至少兩個 CSV。
- 沒有 CSV 時提示正確。
- 輸出檔被 Excel 鎖定時不破壞舊檔，並顯示可理解錯誤。
- Windows Defender／SmartScreen 可能對未簽章自製 EXE 顯示警告，README 應如實說明；正式公司發布建議使用程式碼簽章。

## 14. PyInstaller 封裝規格

第一版建議保留 console 視窗，以便顯示處理結果：

```powershell
pyinstaller --noconfirm --clean --onefile --console `
  --name "CSV報表轉換工具" `
  --icon "assets\app.ico" `
  "src\csv_to_formatted_xlsx\__main__.py"
```

最終應以 `.spec` 固定參數，避免每次封裝結果不同。`scripts/build.bat` 應完成：

1. 建立或使用虛擬環境。
2. 安裝鎖定版本依賴。
3. 執行 pytest。
4. 清理舊 build 產物。
5. 執行 PyInstaller。
6. 將 EXE 複製到發布資料夾。
7. 建立空白 `in/out/logs` 與使用說明。

發布結構：

```text
release/
├─ CSV報表轉換工具.exe
├─ in/
│  └─ CSV檔案放這裡.txt
├─ out/
├─ logs/
└─ 使用說明.txt
```

## 15. GitHub 與版本管理

- 預設分支：`main`。
- 建議使用語意化版本：`v0.1.0` MVP、`v1.0.0` 正式內部使用版。
- 每次 Release 附上 EXE、SHA-256 雜湊及變更紀錄。
- GitHub Actions 至少在 Windows 執行 pytest；正式 EXE 建議由 Windows runner 建置。
- 不提交真實公司報表。測試 fixtures 應為去識別、人工建立的小型資料。

建議 `.gitignore`：

```gitignore
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
build/
dist/
release/
in/*.csv
out/*.xlsx
logs/*.log
*.tmp
```

## 16. README 必須包含

1. 工具用途與條碼問題說明。
2. 三步驟操作圖文：放入、雙擊、取出。
3. 支援的輸入編碼及輸出格式。
4. 原始 CSV 不會被刪除或上傳。
5. 同名檔案的覆蓋規則。
6. 常見錯誤：檔案被占用、編碼不支援、CSV 結構異常。
7. 從原始碼執行、測試及封裝方式。
8. SmartScreen 與程式碼簽章說明。

## 17. 開發階段與完成定義

### Phase 1：專案骨架

- 建立目錄、`pyproject.toml`、依賴、CLI 入口、日誌。
- 完成條件：`python -m csv_to_formatted_xlsx --help` 正常。

### Phase 2：核心轉換

- 完成 CSV 解析、欄位規則、XLSX 寫入、安全覆蓋。
- 完成條件：單元測試及 XLSX 回讀測試通過。

### Phase 3：實際報表驗證

- 使用實際 5,983 列樣本執行端對端測試。
- 完成條件：條碼抽驗與列數、欄數全部通過。

### Phase 4：Windows 封裝

- 建立 spec、build.bat、release 結構。
- 完成條件：乾淨 Windows 電腦無 Python 可操作。

### Phase 5：文件與發布

- 完成 README、使用說明、版本資訊及 GitHub Release。
- 完成條件：非技術主管可只看使用說明獨立完成轉換。

## 18. AI 開發代理執行指令

VS Code AI 讀取本文件後，應遵循：

1. 先檢查現有專案檔案，不覆蓋使用者已完成且正確的內容。
2. 以小步驟完成 Phase 1 至 Phase 5，每階段都執行測試。
3. 不得將條碼欄轉成數字，也不得只靠 Excel 顯示格式掩蓋已發生的精度損失。
4. 每次修改欄位轉型邏輯後，必須重跑前導 0、15 位以上識別碼與公式注入測試。
5. 不得提交使用者真實 CSV 或生成的業務報表至 Git。
6. 如規格有矛盾，以「不改動來源資料、識別碼完整保真、失敗不破壞既有輸出」為優先順序。
7. 完成後回報：已建立檔案、測試結果、EXE 路徑、已知限制及人工驗收步驟。

## 19. 未來擴充方向

- 以 `config.toml` 自訂文字、數值、日期與百分比欄位。
- 簡易拖放 GUI。
- 支援 `.tsv`、分號分隔檔及多 CSV 合併工作表。
- 處理完成後將來源移至 `archive`，但必須由使用者明確啟用。
- 自訂 Excel 樣式範本。
- 大型檔案串流寫入及進度百分比。
- 自動更新機制；公司環境使用時需先完成安全及簽章評估。

## 20. 最終驗收核心句

> 任何看起來像數字、但實際上是識別碼的內容，都必須逐字保留；XLSX 內的條碼必須是真正的文字儲存格，而不是已失真的數字搭配文字顯示格式。
